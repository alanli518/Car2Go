***MAKE SURE PATH FOR BOTH “BulkUpload.sh” ARE CORRECT***

1. Open two instances of ES with runES.sh
2. Open Kibana with runKibana.sh
3. Open Sense and perform dynamic mapping for car2go index using Mapping.json
4. Bulk Upload car2go JSON files using BulkUpload.sh (use "chmod +x $PATH/BulkUpload.sh" to grant execute permission)